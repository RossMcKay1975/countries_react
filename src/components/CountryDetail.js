
import React, {Component} from 'react'

const CountryDetail = ({ name, capital }) => {
    return(
        <div>
        <h4>{name}</h4>
        <h4>{capital}</h4>
        </div>
    )}

export default CountryDetail